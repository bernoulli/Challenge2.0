//
//  CategoriesViewModel.swift
//  Challenge
//
//  Created by Bernardo Cuervo on 26/03/22.
//

import Foundation

/*
func loadCategories(dataJson: dataRequestSendEmail, view: UIViewController, completion: @escaping (_ index: Any) -> Void) {
    
    var jsonString = ""
    let urlGetCategories = apiService.Categories
    
    do {
        let jsonData = try JSONEncoder().encode(dataJson)
        jsonString = String(data: jsonData, encoding: .utf8)!
    } catch _ {
        return
    }

    DispatchQueue.main.async {
        do {
            //print("DATA POST - \(String(decoding: data, as: UTF8.self))")
            let decoder = JSONDecoder()
            //let dataResponse = try decoder.decode(dataRequestSendEmailResponse.self, from: data)
            completion(dataResponse)
        } catch let error {
            completion(error)
            return
        }
    }
}
*/
